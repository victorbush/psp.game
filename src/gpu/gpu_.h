#ifndef GPU__H
#define GPU__H

/*=========================================================
DECLARATIONS
=========================================================*/

// TODO : Make this variable to support triple buffering
#define NUM_FRAMES 2 

typedef struct gpu_s gpu_t;
typedef struct gpu_intf_s gpu_intf_t;

#endif /* GPU__H */